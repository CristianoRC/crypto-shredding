apiVersion: v1
kind: Secret
metadata:
  name: postgres-customers-keys-secret
  namespace: crypto-shredding
type: Opaque
stringData:
  POSTGRES_DB: keyvault
  POSTGRES_USER: postgres
  POSTGRES_PASSWORD: postgres
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-customers-keys-pvc
  namespace: crypto-shredding
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-customers-keys
  namespace: crypto-shredding
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-customers-keys
  template:
    metadata:
      labels:
        app: postgres-customers-keys
    spec:
      containers:
        - name: postgres
          image: postgres:17
          envFrom:
            - secretRef:
                name: postgres-customers-keys-secret
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "postgres"]
            initialDelaySeconds: 3
            periodSeconds: 3
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: postgres-customers-keys-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-customers-keys
  namespace: crypto-shredding
spec:
  selector:
    app: postgres-customers-keys
  ports:
    - port: 5432
      targetPort: 5432
